{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
<div class="newEvent">
   <div class="wilderName"> <h1>Ajouter un noveau événement</h1></div>
    <div class="btns">
    {{ include('event/_form.html.twig') }}

    <a class=" btn btn-primary btn-lg" href="{{ path('event_index') }}">Retour à la liste</a>
    </div>
     </div>
{% endblock %}
