{% extends 'base.html.twig' %}

{% block title %}New Team{% endblock %}

{% block body %}
<div class="newEvent">
      <div class="wilderName">  <h1>Ajouter un noveau wilder</h1></div>
    <div class="btns">
    {{ include('team/_form.html.twig') }}

    <a class=" btn btn-primary btn-lg"  href="{{ path('team_index') }}">Retour à la liste</a>
    </div>
    </div>
{% endblock %}
